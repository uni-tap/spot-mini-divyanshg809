/*var five = require("johnny-five");
var board = new five.Board();

board.on("ready", function() {
  /*var fL = new five.servo(12);
  var fR = new five.servo(11);
  var rL = new five.servo(9);
  var rR = new five.servo(8);*/
 /* var lap = 0;
  var LLs = new five.Servos([12, 8]);
  var RLs = new five.Servos([11, 9]);
  
  LLs.center();
  RLs.center();
    
  this.repl.inject({
     lls: LLs,
     rls: RLs    
  });
var current = {
    action: '';
}
function walk(){
    current.action = "walk";
    LLs.sweep().on("sweep:full", function(){
        console.log("lap", ++lap);
       if(lap === 1){
           this.sweep({
               range:[40, 140],
               step:10
           });
       } 
    });    
}
function stop(){
    current.action = '';
    LLs.stop();
    RLs.stop();
}    
});*/
